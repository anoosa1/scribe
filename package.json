{
  "name": "scribe",
  "version": "1.0.0",
  "description": "Real-time collaborative whiteboard on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "deploy": "vite build && wrangler deploy",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.14.0"
  }
}